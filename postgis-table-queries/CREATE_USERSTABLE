CREATE TABLE USERSTABLE(
    ID              SERIAL  PRIMARY KEY,
    USERNAME        TEXT    NOT NULL UNIQUE,
    PASSWORD        TEXT    NOT NULL,
    MOBILENUMBER    TEXT,
    DESCRIPTION     TEXT,
    ROLES           TEXT,
    JURISDICTION    TEXT,
    EXPIRY          DATE
);
